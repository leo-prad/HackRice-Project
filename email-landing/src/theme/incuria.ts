/**
 * @deprecated Import from `theme/brand` for new code. Retained so existing
 * `incuria-*` imports keep working after the palette unification.
 */
export {
  brand as incuria,
  EASE as motionEase,
  motionDuration,
} from "./brand";
